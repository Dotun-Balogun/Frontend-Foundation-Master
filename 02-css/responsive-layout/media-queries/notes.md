# Media Queries & Fluid Layouts —
## 1️⃣ What Are Media Queries?
Media queries allow CSS to **apply rules only when certain conditions are met** — usually screen width.

Syntax:

```css
@media (max-width: 768px) {
  /* CSS rules here */
}
